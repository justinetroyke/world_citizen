<%= form_for @organization do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :location %>
  <%= f.text_field :location %>

  <%= f.submit 'Add Organization', :class => 'organization-submit' %>
<% end %>
