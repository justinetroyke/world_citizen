<%= form_for @business do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :location %>
  <%= f.text_field :location %>

  <%= f.submit 'Save New Business', :class => 'business-submit' %>
<% end %>
